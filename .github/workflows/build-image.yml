name: Build docker image

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Build image
        run: docker build . --file Dockerfile --tag chat_app:$(date +%s)
        env:
          PORT: $PORT
